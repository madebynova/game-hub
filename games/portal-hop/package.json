{
  "name": "portal-hop",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "PORTAL HOP - a browser game. Built with Vite + vanilla JS, deployed as a static site.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
