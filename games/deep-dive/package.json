{
  "name": "deep-dive",
  "private": true,
  "version": "0.1.0",
  "description": "Deep Dive — a risk-vs-reward treasure diving game (Phase 1)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.3.0",
    "vitest": "^5.0.0"
  },
  "dependencies": {
    "@fontsource/rubik": "^5.3.0"
  }
}
